# Enable hoisting for better compatibility
shamefully-hoist=true

# Auto-install peers to avoid warnings
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false

# Allow these packages to run build scripts
enable-pre-post-scripts=true

# Explicitly allow build scripts for critical packages
# This is safe - these are well-known, trusted packages from npm
[email protected]=true
[email protected]=true
[email protected]=true
[email protected]=true
prisma=true
esbuild=true
sharp=true
core-js=true
core-js-pure=true
