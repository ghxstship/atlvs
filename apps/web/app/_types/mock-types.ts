
// Mock types for missing components
type FieldConfig = {
  key: string;
  label: string;
  type: string;
  required?: boolean;
  sortable?: boolean;
  filterable?: boolean;
};

type DataViewConfig = any;
type DataRecord = any;
