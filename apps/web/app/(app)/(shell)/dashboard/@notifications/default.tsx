/**
 * Default fallback for notifications parallel route
 * Shown when navigating to routes that don't have a matching notifications slot
 */
export default function NotificationsDefault() {
  return null;
}
